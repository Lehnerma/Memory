# Projekt-Doku

Chronologisches Arbeitstagebuch. Menschlich lesbar, keine Memory-DB.

## 2026-07-30

**Settings-Seite: Preview-Bild, Multi-Page-Build, Semantik**

- Ziel: In der Settings-Seite Platz für ein dynamisch austauschbares Theme-Preview-Bild schaffen (Platzhalter `code-vibes/default.png`).
- Dabei aufgefallen: `pages/settings.html` war versehentlich nach `public/pages/settings.html` verschoben worden. Problem: Dateien in `public/` werden von Vite unverändert kopiert, nicht als Build-Entry verarbeitet — der `<script src="../src/main.ts">` wäre dadurch nie zu echtem JS gebündelt worden. HTML-Entry-Punkte gehören ins Projekt-Root (`pages/`), nicht nach `public/`.
- Datei zurück nach `pages/settings.html` verschoben. `vite.config.ts` um `build.rollupOptions.input` erweitert (zwei Entries: `index.html` und `pages/settings.html`), damit Vite beide Seiten korrekt baut. `__dirname` funktioniert im ESM-Config-Kontext nicht — stattdessen `dirname(fileURLToPath(import.meta.url))` verwendet.
- Bildpfad-Frage geklärt (empirisch getestet, nicht geraten): `%BASE_URL%` funktioniert bei `base: ""` **nicht** zuverlässig für verschachtelte HTML-Entries (löst zu `"./"` auf, nicht tiefen-bewusst — führt bei `pages/settings.html` auf einen falschen, nicht existierenden Pfad). Korrekt und getestet (Build + Dev-Server, beide HTTP 200): relativer Pfad `../assets/img/themes/code-vibes/default.png` aus `pages/settings.html` heraus. Faustregel: Inhalte aus `public/` werden immer **ohne** das Präfix `public/` an der Root ausgeliefert.
- HTML-Struktur für Settings-Main jetzt geplant als 2-Spalten-Layout: linke Spalte = ein `<section>` mit den drei Options-Gruppen (Game Themes, Choose Player, Board Size), rechte Spalte = `<section>` mit `<figure>` (Preview-Bild + spätere Zusammenfassung als `<figcaption>`). Semantik-Review ergab: `<article>` war falsch gewählt (kein syndizierbarer Inhalt) → `<section>`; Options-Gruppen sollten `<fieldset>`/`<legend>` statt `<h2>`+`<div>` sein, damit Screenreader Gruppe+Label korrekt announced.
- Theme-Wechsel wird **live per JS** ausgelesen (`change`-Event auf den Radios/Checkboxes), kein `<form>`-Submit nötig.
- Entscheidung Max: Barrierefreiheit hat in diesem Projekt (privates Memory-Spiel) keine hohe Priorität — sauberes semantisches HTML bleibt aber wichtig. In `.claude/CLAUDE.md` hinterlegt.
- Nebenbei entdeckt und behoben: Projekt-`CLAUDE.md` lag unter `claude/CLAUDE.md` (Ordner ohne führenden Punkt) und wurde dadurch von Claude Code nie automatisch geladen. Nach `.claude/CLAUDE.md` verschoben (dort lag bereits `settings.local.json`).

## 2026-08-01

**Issue #16: Radio-Button-Styling in den Settings**

- Branch `radio-button` (bereits vorher angelegt). Issue: [#16](https://github.com/Lehnerma/Memory/issues/16) "Settings - Radio Button Styling", Parent-Issue #8. Anforderung laut Figma: Kreis-Radio mit Punkt in der Mitte bei `:checked`; Label-Text `Almarai` 20px, `font-weight: 400` normal / `700` wenn ausgewählt. Nur CSS, kein JS.
- Für den Codebase-Überblick den `session-code-reviewer`-Subagenten (Haiku) genutzt, um `settings.html`, `_settings.scss`, `_buttons.scss`, `_fonts.scss`, `buttons.ts` etc. zu lesen. Ergebnis: Radios sind native `<input type="radio">` ohne Custom-Styling, Label ist `.settings__field--text` mit `@include u.f-almarai(20)` (nur Größe, kein weight-Wechsel). Das existierende SVG-Line-Toggle über `:not(:checked) + .svg__line { display: none; }` war schon vorhanden und blieb unverändert.
- Figma-Referenzbild aus dem Issue lokal heruntergeladen und angesehen (GitHub user-attachments Link), um das Design zu verifizieren statt zu raten.
- Umsetzung in `src/styles/pages/_settings.scss`:
  - `--text`: `line-height: 100%` ergänzt; neuer Selektor `&:has(.settings__field--radio-btn:checked) { font-weight: 700; }` — Label wird automatisch bold sobald sein Radio-Input checked ist, ganz ohne JS. Voraussetzung: moderner Browser-Support für `:has()` (2026 breit verfügbar).
  - `--radio-btn`: `appearance: none` + `border: 2px solid base.$primary-dark` + `border-radius: 50%` als Ring; `::before`-Pseudo-Element (12px Kreis, `base.$primary-dark`) das bei `:checked` per `transform: scale(0) → scale(1)` eingeblendet wird (nutzt vorhandenes `u.transition`-Mixin).
  - Nicht umgesetzt: `leading-trim: none` aus dem Figma-Spec — kein stabiler CSS-Standard, nur experimentell hinter Chrome-Flags, bewusst weggelassen.
- Verhalten heute: Max wollte für diese Session **keinen Mentor-Modus**, sondern direkte Senior-Dev-Hilfe (Code selbst schreiben, nicht sokratisch fragen). Sichtprüfung im Dev-Server macht Max selbst — nicht proaktiv `npm run dev` starten (siehe Memory `feedback_no_proactive_devserver`).

**7-1-Pattern: Radio-Styles zur eigenen Komponente extrahiert**

- Max wollte die neuen Radio-Styles nicht settings-spezifisch lassen, sondern sauber ins 7-1-Pattern einsortieren (Ordner `base/components/layout/pages/themes/utilities/vendor` + `style.scss` sind bereits als 7-1 angelegt).
- Entscheidung (Rückfrage via AskUserQuestion): neuer generischer BEM-Block `.radio` statt settings-gebundenem `settings__field--text`/`settings__field--radio-btn` — analog zu `.btn` und `.svg`, die schon in `components/` liegen.
- Neue Datei `src/styles/components/_radio.scss`: enthält `.radio` (Label-Wrapper mit Almarai 20px, `:has(.radio__input:checked)` → `font-weight: 700`) und `.radio__input` (Ring + `::before`-Punkt-Kreis, `:checked`-Transition). Die SVG-Line-Toggle-Regel (`:not(:checked) + .svg__line { display: none; }`) ist mit umgezogen, da sie Teil des wiederverwendbaren Radio-Verhaltens ist, nicht settings-spezifisch.
- `components/_index.scss`: `@forward "radio";` ergänzt.
- `pages/_settings.scss`: die alten `&--text`/`&--radio-btn`-Regeln entfernt — `&__field` enthält jetzt nur noch page-spezifisches Layout (Icon-Größe, Title, Options-Gap/Margin).
- `pages/settings.html`: alle 9 Radio-Labels/Inputs von `settings__field--text`/`settings__field--radio-btn` auf `radio`/`radio__input` umbenannt.
- Verifiziert mit `npx vite build` (nur Compile-Check, kein Dev-Server) — SCSS kompiliert fehlerfrei. Dabei aus Versehen `dist/index.html` neu gebaut (dist ist zwar in `.gitignore`, aber aus einem alten Commit noch getrackt) — wieder auf den committeten Stand zurückgesetzt (`git checkout -- dist/index.html`), damit kein unrelated Diff reinrutscht.

## 2026-08-02

**Issue #14: Dynamic Summary — Radio-Auswahl live in der Summary anzeigen + Start-Button-Gate**

- Branch `dynamic_summary` (bereits vorher angelegt). Issue: [#14](https://github.com/Lehnerma/Memory/issues/14) "Dynamic Summary", Parent #8. Verhalten heute: Max hat für diese Session explizit **Senior-Dev-Modus statt Mentor-Modus** angefragt (Code direkt schreiben, für Rückfragen bereitstehen) — abweichend vom sonst geltenden Mentor-Default aus `.claude/CLAUDE.md`.
- Fehlende HTML-IDs ergänzt (`pages/settings.html`): `id="field_player"` / `id="field_boardsize"` auf den bisher unlabelten Fieldsets, `id="summary_theme"` / `id="summary_player"` / `id="summary_boardsize"` auf den drei Summary-`<p>`-Elementen (vorher `id=""`), `id="btn_start"` auf dem Start-Button. Alles snake_case, konsistent zu bestehenden IDs (`btn_play`, `theme_preview`, `field_themes`).
- Neue Datei `src/scripts/summary.ts`: liest pro Fieldset den gecheckten Radio-Button aus (`getCheckedLabelText`), nutzt dafür den sichtbaren Label-Text statt eines `value`-Attributs (Player/Board-Size-Inputs hatten gar keine `value`s) — spart zusätzliche HTML-Änderungen und die Summary zeigt automatisch lesbaren Text ("Blue", "16 Cards" etc.). Kleine Funktionen nach Single-Responsibility: `getCheckedLabelText`, `updateSummaryField`, `updateStartButtonState`, `updateSummary`, `bindRadioChangeListeners`, exportiertes `initSummary()`.
- `initSummary()` guardet auf `document.querySelector(".settings")`, da `index.html` und `pages/settings.html` beide dasselbe `src/main.ts` als Script-Entry einbinden (siehe `vite.config.ts` Multi-Page-Build) — ohne Guard würde die Logik auch auf der Landingpage laufen und dort ins Leere greifen.
- `src/main.ts`: `initSummary()` in `init()` eingehängt, Platzhalter-`console.log('Hello World')` entfernt.
- `src/scripts/buttons.ts`: alten auskommentierten `returnTheme()`-Referenzcode aus dem Issue entfernt, da jetzt durch `summary.ts` ersetzt/abgedeckt.
- Kollision entdeckt: Max hatte parallel schon `src/scripts/input.ts` mit einer Kommentar-Skizze (`inputs abfragen`, `dynamisch einfügen in summary`, `btn active`) angelegt — inhaltlich identisch zu dem, was gerade in `summary.ts` gebaut wurde. Nach Rückfrage: `summary.ts` behalten, `input.ts` gelöscht.
- Verifiziert mit `npx tsc --noEmit` (sauber) und `npx vite build` (kompiliert fehlerfrei); `dist/` danach wieder zurückgesetzt (siehe oben, gleiche Altlast wie bei Issue #16).

**Issue #14 Fortsetzung: zurück in den Mentor-Modus, value/data-label-Refactor, Theme-Bild-Update**

- Max wollte den restlichen Teil von Issue #14 **selbst schreiben**, ich sollte nur bei Fragen helfen — also zurück in den Mentor-Modus laut `.claude/CLAUDE.md` (Ausnahme vom Vortag war nur für Issue #16 gedacht, nicht dauerhaft). Beide CLAUDE.md (global + Projekt) auf Wunsch nochmal explizit gegengecheckt.
- Diskussion Label-Text vs. `value`-Attribut fürs Auslesen der Summary-Anzeige: Label-Text-Parsing (`closest("label")` + Regex) ist implizit an die HTML-Textstruktur gekoppelt und bricht leicht bei Markup-Änderungen; `value`-Attribut ist explizit, aber Theme-Radios nutzten `value` schon als Slug (`coding`, `gaming`, ...) für spätere Bildpfade, nicht als Anzeigetext. Entscheidung Max: **Variante mit zusätzlichem `data-label`-Attribut** — `value` bleibt überall Slug, `data-label` trägt den Anzeigetext. Alle 9 Radio-Inputs in `pages/settings.html` entsprechend ergänzt (Player/Board-Size hatten anfangs noch kein `data-label` bei den 4 Theme-Inputs — per `session-code-reviewer`-Review gefunden und nachgezogen).
- `summary.ts` refactored: neue Hilfsfunktion `getCheckedInput(fieldsetId): HTMLInputElement | null` extrahiert (Fieldset-Suche + `querySelector(':checked')`), damit `getCheckedLabelText` diese Logik nicht mehr dupliziert und jetzt nur noch `checkedInput?.dataset.label` ausliest statt der alten Label-Text/Regex-Variante.
- Dabei ein Typ-Mismatch-Bug entstanden (Parameterübergabe zwischen `getCheckedInput`, `getCheckedLabelText`, `updateSummaryField` nicht konsistent — mal Objekt statt String übergeben, mal umgekehrt) — von Max selbst gefunden und gefixt, per `session-code-reviewer` gegengeprüft.
- Neue Funktionen `getTheme()` und `updateThemeImg()`: lesen den Slug des gecheckten Theme-Radios aus und setzen `src` von `#theme_preview` dynamisch auf `../assets/img/themes/<slug>/default.png`. Mehrere Anläufe nötig (von Max selbst geschrieben, iterativ per Review korrigiert): falscher Pfad-Präfix (`public/` fälschlich mit reingenommen, `../`-Präfix anfangs vergessen), und ein Guard `if (!newImgPath) return` der nie greifen konnte, weil ein Template-String mit `${null}` zu `"null"` gecastet wird und dadurch nie leer/falsy ist — Lerneffekt: Null-Checks müssen auf der Rohquelle (`theme`) passieren, bevor der String gebaut wird, nicht hinterher auf das fertige Ergebnis. Finale Version der Funktion habe ich auf Max' expliziten Wunsch ("stelle du die eine function richtig") direkt geschrieben.
- Für alle Code-Reviews in dieser Session durchgehend den `session-code-reviewer`-Subagenten (Haiku) genutzt, wie in `.claude/CLAUDE.md` vorgeschrieben.
- Offen: Max hatte angekündigt, `summary.ts` perspektivisch auf `settings.ts` umzubenennen (Datei übernimmt jetzt mehr als nur Summary-Logik) — noch nicht umgesetzt.

## 2026-08-03

**Header-Semantik in `pages/board.html`**

- `session-code-reviewer` hat `pages/board.html`, `src/styles/pages/_board.scss`, `src/main.ts` und `src/scripts/buttons.ts` gegengelesen, um zu prüfen ob Header-Elemente (Score-`<div>`s, Current-Player-`<div>`, Exit-Button) bereits per JS/CSS angebunden sind.
- Ergebnis: Nichts davon ist umgesetzt. `_board.scss` ist leer, `buttons.ts` ist leer, `main.ts` bindet nur `initSummary()` (Settings-Seite) ein — der Header ist reines, ungestyltes, unverdrahtetes Markup mit hartcodierten Platzhalterwerten ("0", leeres Div). Keine bestehenden Selektoren, auf die eine Semantik-Überarbeitung Rücksicht nehmen müsste.
- Anschließend Mentor-Gespräch (Mentor-Modus laut Projekt-`CLAUDE.md`) über passende semantische Elemente für den Header-Bereich (Spieler-Anzeige, Current-Player, Exit) statt fertiger Lösung.
- Ergebnis: `<section>` → `<ul>/<li>` für die zwei gleichartigen Spieler, `<figure>`/`<figcaption>` beibehalten, Current-Player von `<h1>` zurück auf `<p>` + `<span>` (Label/Wert-Pattern statt Heading-Missbrauch), Exit-Button frei stehend ohne Section-Wrapper. Auf explizite Bitte ("baue mal um wie du es am sinnvollsten machen würdest") direkt in `pages/board.html` umgesetzt statt nur Fragen zu stellen. Zusätzlich `<img>` in der Current-Player-Div ergänzt, da Max dort später bei Spieler-Wechsel das Bild tauschen will.
- Kurze Nachfrage von Max, ob man den ganzen Header zusätzlich in `<nav>` packen sollte — Antwort: nein, `<nav>` ist nur für Navigationslinks; von den drei Header-Teilen ist höchstens der Exit-Button ein Kandidat (bringt einen weg von der Seite), Scores/Current-Player sind reine Zustandsanzeige und keine Navigation.

**Issue #21 (Memory Board) — Start Styling, 4 Themes (code/gaming/projects/food)**

- Issue-Struktur per `gh issue view` geprüft: #21 ist Parent "Memory Board" mit Sub-Issues #23 (Head-Bereich Styling: Score/Current-Player/Exit), #24 (Board-Grid + Theme-Styling, 3 Board-Größen 4x4/4x6/6x6), #25 (Head-Bereich Logic, blockiert durch #23), #26 (Card-Flip-Logic).
- `session-code-reviewer` hat den Theme-Ordner gegengelesen: `src/styles/themes/_code.scss`, `_gaming.scss`, `_projects.scss`, `_food.scss` sind alle **komplett leer** (nur angelegt als Platzhalter). `_index.scss` hat bereits die `@forward`-Statements für alle 4. `src/styles/pages/_board.scss` ist ebenfalls leer. In `style.scss` ist `@use "themes";` noch auskommentiert (Zeile 13) — 7-1-Grundgerüst (utilities→base→layout→components→pages) ist sonst vorhanden.
- Wichtig festgestellt: Es gibt noch **keine Persistenz** (kein localStorage/sessionStorage) die das auf der Settings-Seite gewählte Theme an board.html weiterreicht — `settings.ts` schreibt den gewählten Theme-Slug aktuell nur in die Preview-Image-src, sonst nirgendwo hin.
- Nächster Schritt: Rückfragen an Max zu Scope (welches Sub-Issue zuerst, #23 oder #24) und Architektur (Theme-Umschaltung über Body-Klasse vs. `data-theme`-Attribut vs. CSS Custom Properties) bevor implementiert wird — noch offen, Antwort steht aus.

**Issue #23 umgesetzt: Theme-System (4 Themes) + Head-Bereich + Exit-Dialog**

- Entscheidung (nach Rückfrage): Theme-Wahl wird via **sessionStorage** von settings.html an board.html weitergereicht (nicht localStorage, da Theme-Wahl Session-State ist, keine dauerhafte Präferenz), Umschaltung über **`data-theme`-Attribut** auf `<body>` + **CSS Custom Properties** pro Theme (nicht Body-Klasse) — BEM bleibt für die Komponentenklassen selbst bestehen. Custom Properties in `src/styles/themes/_coding.scss` / `_gaming.scss` / `_projects.scss` / `_food.scss`, jeweils gescoped auf `[data-theme="x"]`. Rohwerte (Farben) liegen als Sass-Variablen in `src/styles/base/_colors.scss`, universelle Werte (Spieler-Blau/Orange, Weiß) in einem eigenen `// universal`-Block dort.
- Wiederkehrender Sass-Stolperstein, mehrfach aufgetreten und erklärt: Custom-Property-Werte (`--x: ...`) brauchen `#{}`-Interpolation für Sass-Variablen, sonst wird der Wert nicht aufgelöst (ungültiges CSS). Auch `base.`-Modul-Namespace darf nicht vergessen werden.
- `pages/board.html` Header umgebaut: `<ul>/<li>` mit `<figure>` pro Spieler (später wieder vereinfacht auf reines `<img>` + `<span>` durch Max), Current-Player-Bereich mit `<h1>` (Max' bewusste Entscheidung gegen meinen ursprünglichen Vorschlag, da für ihn die wichtigste Info auf dem Screen), Exit-Button mit inline-SVG (`board-header__exit-icon`, `fill: var(--exit-svg-color)`, Vendor-`id` entfernt wegen möglicher Doppel-IDs).
- Neues Muster: fehlende Werte in den Theme-Dateien werden als `null;` deklariert (Sass lässt `null`-Deklarationen beim Kompilieren komplett weg, kein ungültiges CSS) — mit `// TODO`-Kommentar wenn der Wert noch fehlt, ohne Kommentar wenn bewusst leer (z.B. Coding-Back-Button hat bewusst keinen Rahmen/Schatten).
- Rückfall-Ebenen (`var(--x, fallback)`) konsequent eingebaut, u.a. `--header-bg` → fällt auf `--main-bg` zurück, Border-Farben → `transparent`, Shadows → `none`, `--exit-btn-border-width` → `1px`/`3px` als Fallback (damit Food mit `3px`-Stroke themen-spezifisch abweichen kann, ohne dass andere Themes etwas angeben müssen).
- Neue Custom-Property-Familie `--back-btn-*` (Zurück-Button im späteren Exit-Dialog) und `--dialog-exit-btn-*` (eigenständiger Exit-Bestätigen-Button im Dialog, unabhängig vom Header-Exit-Trigger `--exit-btn-*` — nötig, weil z.B. bei Coding derselbe halbtransparente Hintergrund auf dunklem Header anders aussieht als auf weißem Dialog-Grund, wodurch auch die Textfarbe unabhängig gesetzt werden muss).
- `src/styles/base/_fonts.scss`: 3 neue Fonts eingebunden (Figtree, Klee One, Orbitron) nach bestehendem `@font-face`-Muster, Variable-Font-Datei von Orbitron bewusst ausgelassen (Duplikat zu den statischen Schnitten). Font-Zuordnung pro Theme: Coding → Rose, Gaming → Orbitron, Projects → Figtree, Food → Klee One.
- Exit-Bestätigungs-Dialog gebaut (`<dialog class="exit-dialog">` in `board.html`, Styles in `_board.scss`): natives `<dialog>`-Element statt Div+ARIA-Handbau (Fokus-Trap, `::backdrop`, Escape-Handling kommen automatisch mit). Umsetzung nach Figma-Screenshot (`Pop up_01`, Coding-Theme). Text/Hintergrund des Dialogs aktuell noch fix (nicht themeable) — als TODO markiert, da unklar ob das gegen Figma variieren soll.
- Mehrere Bugs beim gemeinsamen Durchgehen gefunden und gefixt: falscher `public/`-Pfad-Präfix bei Asset-URLs (wiederholt aufgetreten, gleicher Fehler wie beim Theme-Preview-Bild aus einer früheren Session), `justify-self: flex-start/flex-end` (ungültig bei Grid-Items, richtig: `start`/`end`), `letter-spacing` mit `%`-Einheit (ungültig, auf `em` umgerechnet) plus falsche `var()`-Fallback-Syntax (Komma gehört in die Klammer), Food-Theme `--main-bg` zeigte fälschlich auf eine Gaming-Variable.
- Neues GitHub Sub-Issue [#30](https://github.com/Lehnerma/Memory/issues/30) angelegt (unter #25): Spieler-Farbe (Blue/Orange) unabhängig vom Theme via `data-player`-Attribut, noch nicht umgesetzt.
- Score-Einfärbung ergänzt: `.player-score--blue`/`.player-score--orange` (Klassen von Max direkt im Markup angelegt) nutzen die universellen `--player-blue`/`--player-orange` Custom Properties.
- Offen/TODO (siehe Kommentare in den 4 Theme-Dateien): diverse fehlende Farbwerte pro Theme, Dialog-Hintergrund/Text themeable oder fix, `--dialog-exit-btn-*` für Coding/Gaming/Food noch ohne Werte.

## 2026-08-05

**Bugfix: Exit-Dialog-Button springt beim Hover**

- Max meldete: der Exit-Button im Bestätigungs-Dialog (`.exit-dialog__btn--exit`) "springt" beim Hover.
- Ursache per `Explore`-Subagent in den Theme-Dateien verifiziert: `border-width` im Normal-State kam aus `var(--exit-btn-border-width, 2px)`, im Hover-State aus `var(--dialog-border-width-hover, 1px)` — zwei unterschiedliche Custom Properties statt einer gemeinsamen. Im Coding-Theme ist `--exit-btn-border-width` gar nicht gesetzt (Fallback 2px), `--dialog-border-width-hover` dagegen explizit auf 3px gesetzt → Breitenwechsel 2px→3px beim Hover, dadurch Layout-Sprung.
- Referenzmuster im selben File gefunden: der Header-Exit-Button (`.board-header__exit`) nutzt in Normal- und Hover-State dieselbe Variable `--exit-btn-border-width` mit demselben Fallback — dort ändert sich nur die Border-**Farbe**, nie die Breite.
- Fix in `src/styles/pages/_board.scss` (`.exit-dialog__btn--exit`): Normal- und Hover-State nutzen jetzt beide `var(--dialog-border-width, 1px)` (gleiche Variable wie beim `--back`-Button im selben Dialog) — Breite bleibt beim Hover konstant, nur Background/Border-Farbe/Shadow ändern sich.
- Nebenbefund (noch nicht behoben): `--exit-btn-border-width` wird nur im `food`-Theme gesetzt, in coding/gaming/projects nicht — vermutlich eine Lücke, sollte gegen Figma geprüft werden. `--back`-Button hat mit `--dialog-border-width` / `--dialog-border-width-hover` strukturell dasselbe Zwei-Variablen-Muster wie der Exit-Button vor dem Fix — im Coding-Theme sind aktuell beide auf 3px gesetzt (kein sichtbarer Sprung), aber die Variablen sind nicht als Paar synchron garantiert. Auf Wunsch von Max direkt umgesetzt statt nur Mentor-Hinweise zu geben.
- Folgeanpassung (gleicher Tag): Designvorgabe ist explizit 2px→3px Border-Wachstum beim Hover für `.exit-dialog__btn--exit` (Designer-Wunsch, laut Max keine Rückfrage nötig). Echte `border-width`-Änderung würde wieder den ursprünglichen Sprung verursachen. Lösung: `border-width` bleibt technisch fix bei `2px` (`var(--dialog-exit-btn-border-width, 2px)`), das dritte Pixel kommt beim Hover als `box-shadow: 0 0 0 1px <farbe>`-Ring obendrauf (nimmt keinen Platz im Box-Model ein, kombiniert per Komma mit dem bestehenden `--exit-btn-shadow`). Optisch nicht von einer echten 3px-Border zu unterscheiden — Achtung aber: Computed Style zeigt weiterhin `2px`, falls das mal exakt gegen Figma/DevTools gemessen wird, ist das ein falsches Signal.

**Refactor: `src/styles/base/_colors.scss` — Primitive-Farbtokens statt duplizierter RGBA-Werte**

- Max' Frage: lohnt sich ein Umbau der Farbvariablen, damit Themes nur noch "Namen" (Primitives) statt wiederholter RGBA-Werte referenzieren, Beispiel-Namensmuster `coding-primary-mint-green`. Empfehlung gegeben (mit Beleg aus der Datei): ja, aber zweistufig — echte 1:1-Duplikate zusammenlegen, bloß ähnliche/abgeleitete Werte (nur andere Opacity/Shade) über Sass `rgba($primitive, $alpha)` aus dem Primitive ableiten statt gleichzusetzen. Max hat sich für eigene Benennung (nicht Figma-Variablennamen) entschieden und mich direkt umsetzen lassen (nicht Mentor-Modus für diesen Schritt).
- Alle vier Theme-Dateien komplett gegengelesen (`Explore`-Subagent) um jede Verwendungsstelle der `base.$...`-Variablen zu erfassen, bevor umgebaut wurde.
- Gefundene exakte Duplikate (identischer RGBA-Wert mehrfach als eigene Variable getippt): Coding (`player-bg`/`exit-btn-bg`; `exit-btn-border`/`border_hover`), Gaming (`btn-border`/`btn-back-border`), Projects (das Dunkelblau `rgba(30,117,148,1)` in 4 verschiedenen Variablen: `btn-bg_hover`, `btn-main`, `font-main`, `btn-back-hover`; das Hellblau in 2), Food (`rgba(243,131,45,1)` in 5 Variablen: `btn-txt`, `btn-border`, `btn-bg-hover`, `btn-back-border`, `btn-back-bg-hover`), sowie themenübergreifend der Schatten `3px 3px 5px 0px rgba(47,46,46,0.2)` identisch in Gaming (`btn-back-shadow`) und Projects (`btn-shadow`, `btn-back-shadow`, `exit-shadow`) und Food (`btn-shadow-hover`) — 5 Stellen, 3 Themes.
- Umsetzung: neuer `// universal`-Block (inkl. `$theme-white`, `$shadow-soft`) an den Dateianfang verschoben (Sass kennt kein Hoisting — Primitives müssen vor ihrer ersten Verwendung stehen). Pro Theme neue Primitive-Variablen ergänzt (`$coding-mint-green`/`$coding-teal`/`$coding-teal-medium`/`$coding-teal-dark`, `$gaming-pink`/`$gaming-magenta`/`$gaming-header-pink`, `$projects-blue-light`/`$projects-blue-dark`/`$projects-coral`/`$projects-coral-dark`, `$food-orange`). Bestehende Komponenten-Variablen (Namen unverändert, damit Theme-Dateien nicht angepasst werden mussten) referenzieren jetzt die Primitives, teils direkt (`$x: $primitive;`), teils per `rgba($primitive, $alpha)` bei abweichender Deckkraft.
- Nebenbefund gefixt: `_coding.scss` hatte als einzige Theme-Datei zwei Farbwerte hart im Theme-File verdrahtet statt über `base.$...` (`--dialog-exit-btn-bg`/`-bg-hover`), inkonsistent zum sonstigen Muster. Jetzt als `$coding-dialog-exit-btn-bg`/`_hover` in `_colors.scss` ergänzt und in `_coding.scss` auf `#{base.$...}` umgestellt. Dabei einen vermutlichen Tippfehler bereinigt: der Hover-Wert hatte `rgba(134, 233, 213, ...)` (213 statt 214 im dritten Kanal) — optisch nicht wahrnehmbarer Unterschied, jetzt konsistent auf `$coding-mint-green` (214) referenziert.
- Verifiziert mit `npx vite build` — kompiliert fehlerfrei. `dist/index.html` (getrackte Altlast) danach wieder auf committeten Stand zurückgesetzt.
- Nicht angefasst, nur notiert: `$coding-player-blue`/`$coding-player-orange` in `_colors.scss` scheinen unused (kein `_coding.scss`-Verweis gefunden, Theme nutzt stattdessen die universellen `$player-blue`/`$player-orange`) — nicht gelöscht, da außerhalb des angefragten Scopes.

**Refactor: `_board.scss` — keine verschachtelten `var(var())`-Ketten mehr**

- Max' Vorgabe: jede Property maximal 1 `var()`-Referenz + optional ein literaler Fallback, keine zwei `var()` mehr in einer Deklaration. Wo eine Variable fehlte, sollte sie neu angelegt werden (mit Default), fehlende Variablen sollten aufgelistet werden. Direktumsetzung gewünscht, kein Mentor-Modus für diesen Schritt.
- `_board.scss`: `background-color: var(--header-bg, var(--main-bg))` → `var(--header-bg, transparent)` (alle 4 Themes setzen `--header-bg` ohnehin explizit, Fallback griff nie). Header-Exit-Button-Border von Shorthand (`border: var(...) solid var(...)`) auf `border-color`/`border-style`/`border-width` aufgesplittet (ein `var()` pro Zeile), analog zum Dialog-Exit-Button-Muster aus der letzten Session. Gleiches für den Dialog-Back-Button-Hover und Dialog-Exit-Button-Hover.
- Fehlende Variablen angelegt (Verhalten dabei unverändert gehalten — jeweils der Wert, den die alte Fallback-Kette vorher schon geliefert hätte):
  - **Coding**: `--exit-btn-txt-hover` (fehlte komplett) → `$coding-exit-btn-txt` (Weiß)
  - **Gaming**: `--exit-btn-border-hover` (war `null`) → `$gaming-btn-border`; `--exit-btn-txt-hover` (fehlte) → `$theme-white`; `--dialog-back-btn-bg-hover` (war `null`) → `$gaming-btn-back-bg`
  - **Food**: `--exit-btn-border-hover` (war `null`) → `$food-btn-border`; `--exit-btn-txt-hover` (fehlte) → `$food-btn-txt`; `--dialog-back-btn-border-hover` (war `null`) → `$food-btn-back-border`
  - **Projects**: keine Ergänzung nötig, dort war schon alles explizit gesetzt oder bewusst `null` (kein Border by design).
- Bewusste Zusatzentscheidung: `color: var(--dialog-exit-btn-txt, var(--exit-btn-border))` fiel vorher auf eine fachfremde Border-Farbe zurück (Zufallstreffer, kein Design-Wert) — stattdessen auf `inherit` als generischen Fallback umgestellt, da `--dialog-exit-btn-txt` in Gaming/Projects/Food noch als TODO offen ist (kein erfundener Farbwert). Gleiches Muster bei `fill: var(--exit-btn-txt-hover, currentColor)` — nutzt jetzt CSS-natives `currentColor` statt einer zweiten Custom Property.
- Nebenbei gefixt (kein 2-Variablen-Fall, aber direkt daneben aufgefallen): `font-size: var(--dialog-font-size)` hatte gar keinen Fallback und die Variable war nur in Coding gesetzt — Gaming/Projects/Food hätten keine Schriftgröße bekommen. Literal-Fallback `u.px-rem(20)` ergänzt (deckt sich mit dem bisherigen Coding-Wert), keine neue Theme-Variable angelegt.
- Bewusst nicht angefasst: `--dialog-exit-btn-bg`, `-bg-hover`, `-shadow`, `-txt` bleiben in Gaming/Food weiterhin mit `// TODO: Wert fehlt noch` — das sind offene Design-Entscheidungen (Farbwerte fehlen), keine Refactoring-Aufgabe.
- Verifiziert mit `npx vite build` (fehlerfrei), `dist/index.html` wieder zurückgesetzt.

**Zwischenruf: Delius-Unicase-Font eingebunden**

- Max bat explizit um einen Subagenten für diese Teilaufgabe. `general-purpose`-Agent hat `DeliusUnicase-Regular.ttf`/`-Bold.ttf` aus `public/assets/fonts/delius-unicase/` als zwei neue `@font-face`-Blöcke in `base/_fonts.scss` ergänzt, exakt nach bestehendem Muster (Pfad ohne `public/`-Präfix, `font-display: swap`, Property-Reihenfolge wie bei Figtree/Klee One/Orbitron/Rose). Bewusst keinem Theme zugewiesen — das sollte Max separat entscheiden. Später von Max selbst in `_food.scss` als `--title-font-secondary: "Delius-Unicase", san-serif;` eingebunden.

**Alle 4 Themes final an Figma angeglichen (von Max selbst gemacht)**

- Max hat `_board.scss`, `_colors.scss` und alle 4 Theme-Dateien eigenständig überarbeitet (viele neue Custom Properties, u. a. getrennte `-hover`-Varianten für Border/Shadow/Text-Color, `--title-font-secondary`, `--dialog-title-*`-Familie). Nicht von mir gegengelesen während der Bearbeitung — erst danach im Rahmen des Cleanups (nächster Absatz) wieder vollständig erfasst.

**Cleanup nach Figma-Angleich: tote Variablen entfernt + `_board.scss` ins 7-1-Pattern einsortiert**

- Auftrag: (1) Codebase auf tote Variablen prüfen und entfernen, (2) `pages/_board.scss` in die passenden 7-1-Ordner aufteilen, (3) Begründung dazu in einer neuen Datei unter `.claude/` dokumentieren. Direktumsetzung gewünscht.
- `session-code-reviewer`-Subagent (Haiku) hat die komplette SCSS-Codebase (alle 24 Dateien unter `src/styles/`) eingelesen und eine Dead-Code-Analyse geliefert — die Analyse der toten **CSS Custom Properties** war bei einem Großteil der Treffer falsch (zu viele strukturell ähnliche Variablennamen wie `--dialog-back-btn-border` vs. `--dialog-back-btn-border-hover` vs. `--dialog-border-width` für ein kleines Modell zuverlässig auseinanderzuhalten). Alle Funde deshalb per `grep` manuell nachverifiziert, bevor irgendetwas gelöscht wurde — Lerneffekt: bei sehr vielen near-identischen Bezeichnern lieber deterministisch grep-verifizieren statt der Modell-Analyse blind vertrauen, gerade bei destruktiven Änderungen (Löschen).
- Tatsächlich entfernt (Details inkl. Begründung stehen jetzt in `.claude/scss-cleanup-und-7-1-aufteilung.md`, nicht hier dupliziert): 4 tote Sass-Variablen in `_colors.scss`, 4 tote CSS-Custom-Properties (Altlasten aus früheren Umbenennungen, z. B. `--dialog-back-btn-txt` ohne das inzwischen genutzte `-color`-Suffix), 1 komplett ungenutztes Mixin `button()` samt der Datei `utilities/_buttons.scss` (nicht zu verwechseln mit `components/_buttons.scss`, das aktiv genutzte `.btn`).
- 7-1-Split: `.exit-dialog` (kompletter Bestätigungs-Dialog inkl. `__text`/`__actions`/`__btn--back`/`__btn--exit`) von `pages/_board.scss` nach neuer Datei `components/_dialog.scss` verschoben — Begründung: ein `<dialog>`-Bestätigungsmuster ist generisches UI, kein Board-spezifischer Inhalt, gleiches Prinzip wie die `.radio`-Extraktion aus einer früheren Session. `.board`, `.board-header*`, `.player-score*` blieben bewusst in `pages/_board.scss` (echter, nicht wiederverwendeter Seiteninhalt). `components/_index.scss` um `@forward "dialog";` ergänzt.
- Nebeneffekt: `@use "../base" as base;` und `@use "../themes" as theme;` waren in `_board.scss` danach ungenutzt (der einzige `base.$primary-dark`-Zugriff ist mit dem Dialog mitgewandert, `theme` war schon vorher tot) — beide Imports entfernt.
- Ausführliche Vorher/Nachher-Begründung (jede verschobene/gelöschte Stelle einzeln) in neuer Datei `.claude/scss-cleanup-und-7-1-aufteilung.md` dokumentiert, wie von Max gewünscht.
- Verifiziert mit `npx vite build` nach Dead-Code-Entfernung UND nach dem 7-1-Split separat (beide Male fehlerfrei, CSS-Output-Größe unverändert bis auf den Dead-Code-Schwund). `dist/index.html` jeweils zurückgesetzt.

## 2026-08-06

**Issue #27: Settings-Auswahl in `sessionStorage` speichern**

- Ziel: die auf der Settings-Seite ausgewählten Werte (Theme, Player, Boardsize) werden bei jeder Änderung in `sessionStorage` persistiert, damit `board.html` sie später auslesen kann. Default-Werte für den Fall eines Direktaufrufs von `board.html` ohne vorherige Settings-Auswahl sind bewusst **nicht** Teil dieses Issues — kommen erst später in der Board-Load-Logik.
- Mentor-Modus laut Projekt-`CLAUDE.md` — Max hat Typen und Struktur selbst erarbeitet, ich hab per Rückfragen geführt: `FieldConfig` um `settingsKey: "theme" | "player" | "boardsize"` (String-Literal-Union) erweitert, neuer Type `Settings` (`{ theme: string; player: string; boardsize: string }`) für den gespeicherten Zustand. `boardsize` bewusst als `string`, nicht `number` — Herkunft ist `HTMLInputElement.value`, das immer ein String ist; eine Zahl-Konvertierung wäre hier verfrüht (YAGNI, betrifft erst die Board-Logik).
- `FIELD_CONFIGS`-Einträge um den jeweiligen `settingsKey`-Wert ergänzt (`"field_themes"` → `"theme"` usw.), damit die bestehende Iterations-Struktur wiederverwendet werden kann statt einer neuen Spezialfunktion pro Feld.
- Neue Funktion `getSettingsKey(fieldId): string | null` (liest `getCheckedInput(fieldId)?.value`) als Pendant zur bestehenden `getCheckedLabelText` (Label-Text vs. Roh-Value).
- Neue Funktion `saveSettings(allSelected: boolean): void`: früher Abbruch wenn `!allSelected` (nur vollständige Auswahl wird gespeichert, kein Teil-Objekt), baut per `FIELD_CONFIGS.reduce(...)` das `Settings`-Objekt zusammen (`{} as Settings` als Startwert, da TS zur Build-Zeit nicht wissen kann dass am Ende alle drei Properties gesetzt sind — Garantie kommt aus dem vorherigen `allSelected`-Gate) und schreibt es via `JSON.stringify` unter dem Key `"gameSettings"` in `sessionStorage`. Aufruf aus `updateSummary()`, wo `allSelected` schon berechnet wird (gleiche Quelle wie für den Start-Button-Gate).
- **Ausnahme vom Mentor-Modus, nur für heute:** Max wollte die Session beenden ("ich will nicht mehr heute") und explizit die fertige Funktion. Da das gegen die feste Regel in `.claude/CLAUDE.md` verstößt, per `AskUserQuestion` rückgefragt (Ausnahme nur heute / Schluss für heute / Regel dauerhaft ändern) — Max hat sich für die einmalige Ausnahme entschieden, Regel in `.claude/CLAUDE.md` bleibt unverändert für morgen. Dabei auch einen Leftover-Debug-Aufruf (`getSettingsKey(...)` ohne Verwendung des Rückgabewerts in `updateSummaryField`) entfernt, der während der Entwicklung stehen geblieben war.
- Verifiziert mit `npx tsc --noEmit` (fehlerfrei).
- Noch offen: Default-Werte beim direkten Aufruf von `board.html` (geplant für später, wenn die Board-Load-Logik gebaut wird).

**Codebase-Review vor Card-Feature-Planung**

- `session-code-reviewer`-Agent (Haiku) hat 7 zentrale Dateien durchgelesen (board.html, board.ts, settings.ts, theme.ts, _board.scss, _gaming.scss, _img.scss) um den Ist-Stand vor dem neuen Card-Feature zu dokumentieren. Keine Code-Änderungen, nur Analyse.
- **Findings**:
  - `<article class="board"></article>` (board.html:35) ist der Platzhalter für das zukünftige Karten-Grid — derzeit leer.
  - SessionStorage-Schema ist sauber (`gameSettings` mit `theme`/`player`/`boardsize`, alle Strings) und wird bereits in settings.ts:60 geschrieben.
  - 3 Board-Größen definiert (settings.html): 16 Cards, 24 Cards, 36 Cards (String-Werte). Keine separate Mapping-Tabelle (z.B. "16" → "4x4") sichtbar — Spalten-Layout fehlt noch.
  - Naming-Konventionen klar: CSS `kebab-case` (BEM mit `__` und `--`), HTML-IDs `snake_case` (z.B. `player_score_blue`), Theme-System via `[data-theme="..."]` + CSS Custom Properties.
  - **Keine Card-Komponenten-Styles vorhanden**: `_board.scss` enthält nur Header-Logik, `_img.scss` nur Preview-Bild-Styles. `.board`-Klasse ist reines leeres Platzhalter-Grid ohne Template-Definition.
  - **Noch nicht implementiert**: Logik zum Auslesen von `boardsize` und entsprechendem Grid generieren, Theme-Bilder-Referenzierung (nur `preview.png` existiert), Card-Element-Struktur.

**Card-Feature-Planung: Struktur + 3 Board-Größen (Flip folgt in eigenem Branch)**

- Branch `feat/creat-cards`, leerer Stub `src/scripts/cards.ts` bereits angelegt (0 Zeilen) — vorgesehener Ort für die neue Karten-Logik.
- Beim Prüfen der Asset-Ordner (`public/assets/img/themes/<theme>/cards/`) zwei Auffälligkeiten gefunden und Max gemeldet (analog `feedback_flag_theme_asymmetry`):
  - `coding` hat nur 17 Kartenmotive (`coding-card1`–`17`), alle anderen Themes (gaming/food/projects) haben 18. Für das 36er-Board (18 Paare) fehlt bei `coding` also ein Bild. Max liefert `coding-card18.png` nach, **bevor** implementiert wird — bis dahin bleibt das ein offener Blocker für coding/36.
  - Ordner heißt `projects`, aber Dateien darin nutzen den Prefix `project-` (Singular) statt `projects-` wie bei den anderen drei Themes — braucht ein explizites Theme→Prefix-Mapping in der Lade-Logik statt der Annahme "Theme-Slug = Datei-Prefix".
- Jedes Theme hat zusätzlich `<prefix>-card-bg.png` (Kartenrückseite, für alle Karten eines Themes identisch) und `<prefix>-match.png` (Treffer-Bild) — beide Assets existieren schon, auch wenn die Verwendung von `match.png` erst mit der Flip/Match-Logik im nächsten Branch relevant wird.
- Entscheidungen (per `AskUserQuestion`, alle von Max getroffen):
  - **Grid**: feste Spaltenzahl pro Board-Größe (kein responsives auto-fill), da reine Desktop-Seite. Layout: 16 → 4 Spalten, 24 → 6 Spalten, 36 → 6 Spalten (Zeilen ergeben sich automatisch über `grid-auto-rows`, keine explizite Zeilenzahl nötig — deckt sich mit Max' ursprünglicher grid-auto-Idee).
  - **Bilder**: sind schon vorhanden (kein Platzhalter-Zwischenschritt nötig), s. Ordnerstruktur oben.
  - **Back-Face**: wird jetzt schon strukturell mitgebaut (nicht erst im Flip-Branch), aber Klick-Handler/Transform-Animation kommt erst später.
  - Eigener Vorschlag zur Back-Face-Umsetzung gemacht (von Max noch nicht bestätigt/verworfen, da Session hier endet): Rückseiten-Bild NICHT als eigenes `<img>` pro Karte einfügen (36x identisches Bild), sondern einmalig als CSS `background-image` (z.B. via Custom Property `--card-back-img` auf `.board`, vererbt an `.card__face--back`) — weniger DOM-Knoten, kein 36-facher `src`-Zugriff via JS, sauberer semantisch (Deko-Bild gehört ins CSS, nur das einzigartige Front-Motiv bekommt ein echtes `<img>`).
- Vereinbarter Plan-Umriss (Umsetzung noch nicht begonnen): (1) Theme→Prefix-Mapping + dynamische Bild-Pool-Auslese in `cards.ts`, (2) Paare erzeugen (boardsize → 8/12/18 Paare) + shuffeln, (3) Card-Markup (`.card` → `.card__button` → `.card__face--front`/`--back`), (4) DOM-Rendering ins `<article class="board">` inkl. `data-boardsize`-Attribut, (5) neue `components/_card.scss` (BEM) + Spalten-Mapping in `pages/_board.scss` über `.board[data-boardsize="..."]`, (6) Verifikation aller 3 Größen × 4 Themes im Browser (coding/36 bleibt bis Bild-Nachlieferung unvollständig).
- Mentor-Modus: Umsetzung startet bei Schritt 1, sobald Max grünes Licht gibt bzw. das 18. coding-Bild geliefert hat.

**Card-Feature Schritt 1: Theme + Bildpfade in `cards.ts`**

- Blocker aufgelöst: `coding-card18.png` nachgeliefert (18/18), `projects`-Ordner von Max selbst auf einheitlichen Prefix `projects-card*` umbenannt (statt `project-`) — Theme→Prefix-Mapping aus dem Plan damit **nicht mehr nötig**, Theme-Slug = Datei-Prefix funktioniert jetzt für alle 4 Themes gleich.
- Wichtige Korrektur während der Erklärung: `import.meta.glob` (ursprünglich als Lösung fürs "dynamische Bildanzahl auslesen" vorgeschlagen) funktioniert **nicht** für unsere Karten-Bilder, da sie unter `public/` liegen — Vite bindet `public/` bewusst nicht in seinen Modul-Graphen ein, nur `src/` ist glob-fähig. Da jetzt alle Themes einheitlich 18 Bilder haben, reicht eine einfache Zahlen-Schleife mit Template-String statt Laufzeit-Erkennung.
- Mentor-Dialog über mehrere Iterationen (Max hat den Code selbst geschrieben, ich hab per Rückfragen/Review geführt):
  - `getTheme()`: erst eigene sessionStorage-Parse-Logik geschrieben → per Rückfrage Duplikat zu `theme.ts` aufgedeckt (dort gab es bereits `getGameSettings()`, nur nicht exportiert) → Max hat `getGameSettings()` selbst exportiert und `getTheme()` darauf umgebaut (`theme.ts:7` jetzt `export function getGameSettings()`).
  - `getBoardSize()`: erster Entwurf mit Rückgabetyp `string | number` und ungültigem Fallback `12` (keine der drei Board-Größen 16/24/36) → nach Erklärung zu `Number`/`parseInt`-Konvertierung selbst auf sauberes `number` mit Fallback `16` umgebaut.
  - `getCardImgPaths()`: Bug gefunden — Loop lief bis `size` (Kartenanzahl, z.B. 36) statt bis `size / 2` (Paar-/Motivanzahl, max. 18 pro Theme) → hätte bei boardsize 24/36 nicht-existierende Dateipfade wie `coding-card19.png` erzeugt. Weitere Funde: fehlender Return-Type/`return` (Ergebnis ging nirgends hin, nur `console.log`), toter Rest `CARD_IMG_PATH` mit dem wiederholt aufgetretenen `public/`-Präfix-Bug, Parametername `number` (überschreibt den TS-Type), lokale Variable `PATHS` in SCREAMING_SNAKE_CASE (inkonsistent zu den camelCase-Nachbarn). Max hat den Loop-Bug, Return-Type und `return paths` selbst gefixt.
- **Konflikt Projekt-Regel vs. Live-Anfrage** (Muster aus `feedback_claude_md_rule_conflict`): Max bat direkt um Code-Review von `cards.ts` durch mich, was der festen Projekt-Regel "Code-Review immer via `session-code-reviewer`" widerspricht. Per `AskUserQuestion` rückgefragt statt selbst entschieden — Max wollte diesmal explizit direktes Review von mir (schneller, Datei eh schon geladen).
- **Naming-Konvention-Auftrag für den Subagenten**: Max wollte zunächst "alle Variablen in UPPERCASE in allen ts-Files" — nach Widerspruch (das ist unüblich, SCREAMING_SNAKE_CASE ist nur für echte Modul-Konstanten üblich, nicht für jede lokale Variable) per `AskUserQuestion` präzisiert: Standard-TS-Konvention (Funktionen camelCase, Klassen/Interfaces/Typen PascalCase, Konstanten UPPER_CASE, lokale Variablen bleiben camelCase). `session-code-reviewer`-Subagent beauftragt, das in allen `src/scripts/*.ts` zu prüfen/fixen (reiner Naming-Pass, keine Logik-Änderungen).
- **Wichtiger Zuverlässigkeits-Fund beim Subagenten-Ergebnis** (Ergänzung zu `feedback` aus der SCSS-Cleanup-Session — gleiches Muster nochmal bestätigt): Der Subagent behauptete (a) `src/scripts/board.ts` und `src/main.ts` würden "nicht existieren" und (b) es gäbe einen Syntaxfehler `function getCardImgPaths():sti {` in `cards.ts`. Beides frei erfunden — per direktem `git diff`/`ls`/`npx tsc --noEmit`-Check verifiziert: beide Dateien existieren (waren zu dem Zeitpunkt sogar schon von Max bzw. dem Subagenten selbst modifiziert), `cards.ts` hat den korrekten Type `string[]`, kein Syntaxfehler, `tsc --noEmit` läuft fehlerfrei durch. Tatsächliche Subagent-Leistung: `PATHS` → `paths` in `cards.ts` korrekt umbenannt, restliche Dateien zu Recht als bereits konform gemeldet. **Lerneffekt**: Haiku-Subagent-Aussagen über Datei-Existenz und Fehler-Diagnosen nicht ungeprüft übernehmen, gerade bei "Datei existiert nicht"/"Syntaxfehler"-Behauptungen — kurzer `ls`/`git diff`/Compiler-Check deckt das zuverlässig auf und kostet kaum Zeit.
- Stand danach: `getTheme()`, `getBoardSize()`, `getCardImgPaths()` fertig und kompilieren fehlerfrei, liefern die Pfade der `size/2` benötigten Kartenmotive pro Theme. Task 1 (Bild-Pool-Auslese) damit inhaltlich abgeschlossen — Duplizieren zu Paaren + Mischen (Task 2) noch offen.

## 2026-08-10

**Endscreen (Issue #41) — Planungssession, noch keine Zeile Code**

- Kontext selbst gelesen (kein Subagent, gezielte `grep`/`cat`-Auszüge statt Volltext): `game-logic.ts`, `board.ts`, `theme.ts`, `main.ts`, `settings.ts` (Ausschnitt), `pages/board.html`, `vite.config.ts`, `pages/_index.scss`, `public/assets`-Baum. Grundlage: Screenshot der drei Figma-Screens (Game over_01, Winner_01 blau/orange, Draw_01) + Issue-Text #41.
- **Funde/Einwände, die den Plan geprägt haben:**
  - **Spielende existiert im Code gar nicht.** `game-logic.ts` hat keinen "alle Karten gematcht"-Check. Der Trigger ist damit Etappe 1, nicht der Screen. Im Issue-Text fehlt dieser Task komplett.
  - **`vite.config.ts` kennt `board.html` nicht** — Input-Liste enthält nur `index.html` + `pages/settings.html`. Im Dev-Server unsichtbar, beim `build` fliegt board.html raus. Muss beim Anlegen der Endscreen-Seite mitgefixt werden.
  - **Neue Seite ⇒ State-Verlust**: Scores/Gewinner leben nur im Modul-RAM. Transport über `sessionStorage` nötig, nach Vorbild von `saveSettings()` — aber unter eigenem Key, `gameSettings` nicht überschreiben.
  - **`main.ts` ruft alle Inits auf jeder Seite auf**, gerettet nur durch `if (!document.querySelector(".board")) return;`-Guards in den Modulen. Endscreen bekommt denselben Guard-Stil (`.endscreen`); echter Page-Router wäre ein eigenes Issue.
  - **`initBoardTheme()` gated auf `.board`** → Endscreen bekäme kein `data-theme`. Guard muss verallgemeinert werden, wenn der Endscreen themed sein soll.
  - **Design vs. Issue widersprüchlich beim Draw**: Issue sagt "beide Schachfiguren", Screenshot zeigt eine Waage.
  - **Confetti/Girlande-Frage aus dem Issue technisch beantwortet**: Widescreen-Erweiterung geht sauber nur, wenn das Asset als horizontal nahtloses SVG exportiert wird (`background-repeat: repeat-x`). Fixes PNG ⇒ harte Kanten oder Unschärfe beim Verbreitern. Muss beim Export geklärt werden, bevor CSS dafür entsteht.
  - Randnotiz: "Blue 10 / Orange 16" im Figma sind Dummy-Werte (26 Punkte gehen bei keiner Boardgröße auf, max. 18 Paare).
- **Entscheidungen (per `AskUserQuestion`, alle von Max):**
  - **Ansatz**: neue Seite `pages/endscreen.html` (nicht Dialog) — Vollbild-Design, Projekt ist ohnehin Multi-Page.
  - **Screen-Aufbau**: *eine* Seite mit zwei Stufen über `data-stage`. Stufe 1 "Game over" + Punktestand, nach ~2s automatisch (setTimeout, kein Button) Stufe 2 mit Confetti + Winner/Draw + "Back to start". Max hat statt einer Option die User Story geliefert ("Als Benutzer erwarte ich nach Beendigung der Runde eine entsprechende Meldung sowie die Möglichkeit, eine neue Runde zu beginnen. Es erscheint eine 'Game over' Anzeige mit aktuellem Punktestand. Der Spieler mit den meisten Punkten wird als Gewinner angezeigt."), daraus die zweistufige Lesart abgeleitet und rückbestätigt.
  - **Draw-Icon**: Waage (Screenshot gewinnt gegen Issue-Text) — Issue #41 entsprechend korrigieren.
  - **"Back to start"** → `settings.html`. Damit ist implizit auch der offene TODO in `board.ts:15` (Exit-Dialog: index vs. settings) in dieselbe Richtung entschieden.
  - **Styling-Ansatz** (Max' eigene Idee, bestätigt): Varianten über data-Attribute (`data-result="blue|orange|draw"`, `data-stage`) statt BEM-Modifier-Klassen — konsistent mit dem bestehenden `data-theme`/`data-boardsize`-Muster.
- **Vereinbarter Weg in 7 Etappen** (Max arbeitet sie einzeln ab, Mentor-Modus — keine Lösung vorgeben):
  1. Spielende erkennen in `game-logic.ts` (alle `isMatched`?), Aufruf aus `resolveMatch()` nach `countScore()`, mit Delay damit das letzte Paar sichtbar bleibt. Nur `console.log` als Beweis.
  2. Gewinner ermitteln: reine Funktion Scores → `"blue" | "orange" | "draw"`, eigener Union-Type.
  3. Ergebnis nach `sessionStorage` (Gewinner + beide Scores, eigener Key), dann `location.href` auf die neue Seite.
  4. `pages/endscreen.html` mit statischem Dummy-Markup + Vite-Input-Liste erweitern (**inkl. board.html**). Semantik: `<main>`, `<h1>`, Score als `<dl>`/`<ul>` statt Div-Suppe.
  5. `src/styles/pages/_endscreen.scss` (BEM-Block `endscreen`, Varianten via `[data-result]`/`[data-stage]`, Farbe+Figur als CSS-Variablen), `@forward` in `pages/_index.scss`. Confetti bleibt Platzhalter.
  6. Neues Modul `endscreen.ts`: sessionStorage lesen, `data-result` setzen, Scores eintragen, Stage-Timer, Back-Button; Guard auf `.endscreen`, Registrierung in `main.ts`.
  7. Assets & Widescreen: Confetti-SVG, Content `max-width: 1040px`, Confetti über volle Breite.
- Etappe 1–3 sind design-unabhängig und waren sofort startklar; Confetti-Asset muss Max noch aus Figma exportieren.
